import React, { Component } from "react";
import BarChart from "./BarChart.png";

export default class PredictionsV extends Component {
    render() {
        return (
            <div>
                <img src={ BarChart } alt="Bar Chart"/>
            </div>
        )
    }
}